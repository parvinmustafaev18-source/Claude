# build.bat и sync.bat сохранены в CP866 с переводами строк CRLF — так требует
# cmd.exe. Помечены как бинарные, чтобы git не трогал ни байта: при конвертации
# переводов строк в LF cmd начинает резать строки посреди слов и файл ломается.
*.bat -text
