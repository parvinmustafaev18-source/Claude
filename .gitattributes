# build.bat сохранён в CP866 с переводами строк CRLF — так требует cmd.exe.
# Помечен как бинарный, чтобы git не трогал ни байта: при конвертации
# переводов строк в LF cmd начинает резать строки посреди слов и файл ломается.
build.bat -text
